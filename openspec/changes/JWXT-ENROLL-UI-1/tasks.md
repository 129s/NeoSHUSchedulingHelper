# Tasks: JWXT-ENROLL-UI-1

- [x] T-1 移除规划交互：JWXT「选课」列表只保留“直接选课”（真实写入 JWXT + 弹窗确认）。
- [x] T-2 复用 CourseFiltersToolbar + AppPagination，并在 JWXT「选课」列表强制分页。
- [ ] T-3 SQL 查询路径（QueryLayer）接入与回退策略（性能优化；本次未实现）。
- [x] T-4 i18n 自检与 `npm --prefix app run check`（记录到 apply.md）。
- [x] T-5 批量复选框：JWXT「选课（直接）」支持批量直接选课；「教务已选」支持批量直接退课（弹窗确认）。
