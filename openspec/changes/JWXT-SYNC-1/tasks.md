# Tasks: JWXT-SYNC-1

- [x] T-1 增强“从教务同步”的本地映射：支持按 `jxb_id -> sectionId` fallback。
- [x] T-2 为 push 增加安全校验：发现远端 kch 与本地 courseCode 不一致时拒绝执行。
- [x] T-3 `npm run check` 记录到 apply.md。
- [x] T-4 对齐 `docs/STATE.md`：Remote Snapshot card 提供“复制到本地 Wishlist/待选”并写入 termState.selection（仅本地追踪）。
